'use strict';
window.P4Rules={monthIncome:{required:true},annualIncome:{required:true},federalRate:{max:100},stateRate:{max:100},bufferRate:{max:100}};P4.bind(({n,put,percent,details,form,K})=>{const month=Math.max(0,n('monthIncome')-n('monthExpenses')),annual=Math.max(0,n('annualIncome')-n('annualExpenses')),se=K.selfEmploymentTax(annual,form.elements.filingStatus.value,n('w2Wages')).total,fed=annual*n('federalRate')/100,state=annual*n('stateRate')/100,base=Math.max(0,se+fed+state-n('covered')),buffer=base*n('bufferRate')/100,total=base+buffer,ratio=annual?Math.min(1,total/annual):0,current=month*ratio;[['monthNet',month],['annualNet',annual],['seReserve',se],['federalReserve',fed],['stateReserve',state],['buffer',buffer],['totalReserve',total],['currentReserve',current],['remaining',Math.max(0,month-current)],['monthlyTarget',total/12],['quarterlyTarget',total/4]].forEach(([id,v])=>put(id,v));put('reservePct',ratio,percent);details([`Annual net income ${K.money(annual)} drives the SE-tax and user-entered federal/state reserves.`,`Covered payments ${K.money(n('covered'))} are subtracted before the ${n('bufferRate')}% safety buffer.`,`Current-payment reserve = monthly net ${K.money(month)} Ã— ${percent(ratio)} = ${K.money(current)}.`]);return{summary:`1099 cash reserve estimate\nCurrent reserve: ${K.money(current)}\nAnnual target: ${K.money(total)}\nNot a safe-harbor determination.`}});
