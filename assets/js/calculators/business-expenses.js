'use strict';
window.P4Rules={grossIncome:{required:true},mixedUsePct:{max:100},federalRate:{max:100},stateRate:{max:100},seRate:{max:100}};P4.bind(({n,put,details,K})=>{const entered=Array.from({length:10},(_,i)=>n('expense'+i)).reduce((a,b)=>a+b,0),eligible=Math.max(0,entered*n('mixedUsePct')/100-n('reimbursements')-n('nondeductible')),fed=eligible*n('federalRate')/100,state=eligible*n('stateRate')/100,se=eligible*n('seRate')/100,total=fed+state+se;[['enteredExpenses',entered],['reimbursementResult',n('reimbursements')],['excludedPortion',entered-eligible],['deductibleAmount',eligible],['incomeBefore',n('grossIncome')],['incomeAfter',n('grossIncome')-eligible],['federalEffect',fed],['stateEffect',state],['seEffect',se],['totalEffect',total],['outOfPocket',Math.max(0,entered-n('reimbursements')-total)]].forEach(([id,v])=>put(id,v));details([`Entered expenses ${K.money(entered)} Ã— ${n('mixedUsePct')}% business use âˆ’ reimbursements âˆ’ nondeductible portion = ${K.money(eligible)} preliminary planning amount.`,`User-entered tax effects total ${K.money(total)}. A ${K.money(entered)} purchase does not create a ${K.money(entered)} refund.`]);return{summary:`Expense deduction planning estimate\nEntered costs: ${K.money(entered)}\nPreliminary deduction: ${K.money(eligible)}\nEstimated tax effect: ${K.money(total)}`}});
