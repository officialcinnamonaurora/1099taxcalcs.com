'use strict';
window.P4Rules={personalPay:{required:true},weeks:{required:true,greaterThanZero:true},hours:{required:true,greaterThanZero:true},billablePct:{required:true,max:100},seRate:{max:100},federalRate:{max:100},stateRate:{max:100},margin:{max:100}};P4.bind(({n,put,percent,details,K})=>{const timeOff=(n('vacationDays')+n('sickDays')+n('holidays'))*8,available=Math.max(0,n('weeks')*n('hours')-timeOff-n('adminHours')),billable=available*n('billablePct')/100,over=n('expenses'),benefits=n('health')+n('retirement')+n('benefits'),base=n('targetRevenue')||n('personalPay')+over+benefits,rates=(n('seRate')+n('federalRate')+n('stateRate'))/100,tax=base*rates,revenue=base+tax,breakEven=billable?revenue/billable:0,recommended=breakEven*(1+n('margin')/100);[['availableHours',available],['billableHours',billable],['overhead',over],['benefitCost',benefits],['taxReserve',tax],['revenueNeeded',revenue],['breakEvenRate',breakEven],['recommendedRate',recommended],['dailyRate',recommended*8],['weeklyRate',recommended*n('hours')],['monthlyTarget',recommended*billable/12],['annualTarget',recommended*billable]].forEach(([id,v])=>put(id,v));details([`Available hours = weeks Ã— hours âˆ’ time off âˆ’ nonbillable administration = ${available.toFixed(1)}.`,`Billable hours = ${available.toFixed(1)} Ã— ${n('billablePct')}% = ${billable.toFixed(1)}.`,`Revenue needed ${K.money(revenue)} Ã· billable hours = ${K.money(breakEven)} per hour.`,`Contingency increases the recommended rate to ${K.money(recommended)}.`]);return{summary:`Contractor rate estimate\nBillable hours: ${billable.toFixed(1)}\nRecommended hourly rate: ${K.money(recommended)}`}});
